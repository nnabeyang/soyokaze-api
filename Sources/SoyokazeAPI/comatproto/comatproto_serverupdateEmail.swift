//
// DO NOT EDIT
//
// Generated by swift-atproto
//

import SwiftAtproto
import Foundation

extension comatprototypes {
    public struct ServerUpdateEmail_Input: Codable, Sendable {
        public var email: String
        public var emailAuthFactor: Bool?
        public var token: String?
        public var _unknownValues: [String: AnyCodable]

        public init(email: String, emailAuthFactor: Bool? = nil, token: String? = nil) {
            self.email = email
            self.emailAuthFactor = emailAuthFactor
            self.token = token
            self._unknownValues = [:]
        }

        enum CodingKeys: String, CodingKey {
            case email
            case emailAuthFactor
            case token
        }

        public init(from decoder: any Decoder) throws {
            let keyedContainer = try decoder.container(keyedBy: CodingKeys.self)
            self.email = try keyedContainer.decode(String.self, forKey: .email)
            self.emailAuthFactor = try keyedContainer.decodeIfPresent(Bool.self, forKey: .emailAuthFactor)
            self.token = try keyedContainer.decodeIfPresent(String.self, forKey: .token)
            let unknownContainer = try decoder.container(keyedBy: AnyCodingKeys.self)
            var _unknownValues = [String: AnyCodable]()
            for key in unknownContainer.allKeys {
                guard CodingKeys(rawValue: key.stringValue) == nil else {
                    continue
                }
                _unknownValues[key.stringValue] = try unknownContainer.decode(AnyCodable.self, forKey: key)
            }
            self._unknownValues = _unknownValues
        }

        public func encode(to encoder: any Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(self.email, forKey: .email)
            try container.encodeIfPresent(self.emailAuthFactor, forKey: .emailAuthFactor)
            try container.encodeIfPresent(self.token, forKey: .token)
            try _unknownValues.encode(to: encoder)
        }
    }

    public indirect enum ServerUpdateEmail_Error: XRPCError {
        case expiredtoken(String?)
        case invalidtoken(String?)
        case tokenrequired(String?)
        case unexpected(error: String?, message: String?)

        public init(error: UnExpectedError) {
            switch error.error {
            case "ExpiredToken":
                self = .expiredtoken(error.message)
            case "InvalidToken":
                self = .invalidtoken(error.message)
            case "TokenRequired":
                self = .tokenrequired(error.message)
            default:
                self = .unexpected(error: error.error, message: error.message)
            }
        }

        public var error: String? {
            switch self {
            case .expiredtoken:
                return "ExpiredToken"
            case .invalidtoken:
                return "InvalidToken"
            case .tokenrequired:
                return "TokenRequired"
            case let .unexpected(error, _):
                return error
            }
        }

        public var message: String? {
            switch self {
            case let .expiredtoken(message):
                return message
            case let .invalidtoken(message):
                return message
            case let .tokenrequired(message):
                return message
            case let .unexpected(_, message):
                return message
            }
        }
    }
}
extension XRPCClientProtocol {
    public mutating func ServerUpdateEmail(input: comatprototypes.ServerUpdateEmail_Input) async throws -> Bool {
        let params: Bool? = nil
        do {
            return try await fetch(endpoint: "com.atproto.server.updateEmail", contentType: "application/json", httpMethod: .post, params: params, input: input, retry: true)
        } catch let error as UnExpectedError {
            throw comatprototypes.ServerUpdateEmail_Error(error: error)
        }
    }
}
